# Frontend Dockerfile
FROM node:20-alpine

# 작업 디렉토리 설정
WORKDIR /app/frontend

# package.json과 package-lock.json 복사
COPY app/frontend/package*.json ./

# 의존성 설치
RUN npm ci

# 애플리케이션 코드 복사
COPY app/frontend ./

# Next.js 빌드 (프로덕션용)
# RUN npm run build

# 포트 노출
EXPOSE 3000

# 개발 서버 실행
CMD ["npm", "run", "dev"]
